export const metadata = {
  title: 'Predator Dashboard | Live Trading Monitor',
  description: 'Real-time monitoring dashboard for the Predator XAUUSD scalping system. Track price action, market regime detection, and agent health.',
};

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
